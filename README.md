### Ollama
```brew install ollama ```
```ollama serve```

### enabled virtual env
- enable venv

### install dependencies 
```pip install -r requirements.txt```

### run the app  
```steamlit run app.py ```
